<h2>Edit profile</h2>

<%= render "form.html", changeset: @changeset,
                        action: profile_path(@conn, :update, @profile, @params) %>

<%= link "Back", to: profile_path(@conn, :index, @params) %>
