<h2>Listing cats</h2>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for cat <- @cats do %>
    <tr>
      <td><%= cat.name %></td>

      <td class="text-right">
        <%= link "Show", to: cow_to_cat_path(@conn, :show, @cow, cat), class: "btn btn-default btn-xs" %>
        <%= link "Edit", to: cow_to_cat_path(@conn, :edit, @cow, cat), class: "btn btn-default btn-xs" %>
        <%= link "Delete", to: cow_to_cat_path(@conn, :delete, @cow, cat), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "New cat", to: cow_to_cat_path(@conn, :new, @cow) %>
